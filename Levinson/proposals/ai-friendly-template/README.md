<!--
====================================================================
NOTE TO READER

This folder is a usable project starter — clone it as the root of
a new repository and follow `GETTING-STARTED.md` to populate it.
You can also browse it as a reference alongside the proposal doc
at ../../docs/2026-05-22-standard-proposal.md.

The rest of this file (below) is the actual template content —
what a real project's README.md would look like when filled in.
Replace it with your project's real README once you've scaffolded
your stack.
====================================================================
-->

<!-- [TEMPLATE] Replace this with your actual project description. Link to your summary doc here. -->

# Project Name

## What this project is
<!-- One short paragraph: what does this do, who uses it, what is it built with. -->
[Describe the project in plain language. Example: "A multilingual content site for [audience], built with [framework] and [CMS]. Currently in [stage]."]

The full project context lives in `SUMMARY.md`.

## How to run it
<!-- The commands a new developer or AI agent needs on day one. Keep this short. -->
```
npm install
npm run dev
```

- Dev server: `http://localhost:XXXX`
- Build for production: `[your build command, e.g. npm run build]`
- Verify a change: `[your verify command, e.g. npm run check]`

## Where to find more
<!-- Don't duplicate content from other docs — link to them. -->
- `CLAUDE.md` — working rules for AI coding agents
- `SUMMARY.md` — current project state and architecture decisions
- `docs/decisions/` — record of *why* the project is built the way it is
- `CHECKLIST.md` — AI-readiness checklist for this repo

## Who works on it
<!-- People + roles. Include AI agents if they are a regular part of the workflow. -->
- [Name] — owner / product
- [Name] — engineering
- Claude Code — implementation partner (rules in `CLAUDE.md`)
